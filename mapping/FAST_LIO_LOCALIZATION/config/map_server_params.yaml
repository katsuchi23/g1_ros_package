map_server:
  ros__parameters:
    use_sim_time: false
    # Override QoS to match RViz default (VOLATILE durability)
    # This ensures RViz can subscribe and receive the map
    qos_overrides:
      /map:
        publisher:
          durability: volatile
          reliability: reliable
